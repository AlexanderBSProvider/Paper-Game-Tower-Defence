# секрети інструментів
tools/.env.local

# кеш AI-генерації (відтворюваний, платити двічі не треба, але в репо не тягнемо)
art/.cache/

# зібране tsc: збирає CI, у репо не тримаємо (інакше dist розходиться зі src)
dist/
*.tsbuildinfo
# артефакт Pages — з'являється, якщо відтворювати білд CI локально
_site/

node_modules/
*.log
.DS_Store
Thumbs.db
