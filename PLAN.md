# TD: Monster Horde — Pixi.js, гра на аркуші зошита

## Context

Скетч у зошиті в клітинку: згори лізуть монстри, унизу оборона, по боках дерева. Гра — Tower Defence, у якій **сам аркуш і є артом**: усе намальовано ручкою на клітинці. Користувач малює на папері, фотографує телефоном; я витягую чорнило, доводжу до єдиного стилю (локально + Gemini image API), збираю cut-out риг і оживляю.

**Зміна концепції (30.07.2026):** траси більше не існує як авторського об'єкта. У гравця дві тулзи — **ручка** і **ластик**, а **чорнило — це валюта**. Ручкою малюєш і башти, і перешкоди; перешкоди зводять лабіринти й вузькі проходи. Ластик стирає що завгодно і повертає половину чорнила. Маршрут ворогів — **наслідок** намальованого, а не заготовка.

**Зміна концепції (11.08.2026) — де хук.** Кроки 1–8 зробили гру грабельною, але механічно це був Desktop Tower Defense зі скіном. Розбір показав: хук має бути в тому, **як народжується башта**, а каркас лишається класичним, як у Kingdom Rush — хвилі, апгрейди, глибина в білді. Гравець має відчувати одночасно «це моє» і «я їх мелю».

**Рішення: башта не має рівнів, вона має склад, і кожну деталь гравець обводить рукою.** Деталі беруться з набору (їх малюємо ми), тому потворного на аркуші не буває; лінію веде палець, тому кожна деталь справді намальована. Точність обведення — множник до статів.

> **Що ставити і куди — це білд. Як воно ляже на папір — це скіл.**

Дві версії відкинуто, і причини варто памʼятати:
- **Вільне малювання пальцем** — гравець креслить будь-які каракулі, аркуш заростає гівном, а «будь-який малюнок валідний» означає, що старатись нема за чим.
- **Чистий конструктор із меню** — виглядає добре, але малювання зникає з гри зовсім, а воно і є весь задум.

Деталі позначаються тегами й мають **сокети**, тому башта росте вгору й убік, а **позиція деталі впливає на стати**: ствол вище — бачить далі. Плюс комбо: пара тегів дає третій ефект, якого немає в жодної деталі окремо.

**Рішення користувача (зафіксовані):**
- Обведення — у **великій рамці**, потім малюнок стискається на місце.
- **Домальовування до готової башти — це і є апгрейд.**
- Магічна й гармата лишаються як **шаблони**: береш заготовку й дороблюєш своє.
- Що гра вичитала — **нотатка на полях** одразу після створення.
- Стіни лишаються, але **дешеві й другорядні**: лабіринт більше не головне рішення партії.
- Pixi.js, 2D, аркуш на весь екран, HUD — нотатки на полях.
- Ракурс зверху-збоку, персонажі анфас.
- Анімація cut-out частинами, процедурні модифікатори зі слайдерами в редакторі.
- Палітра: синя=свої, червона=вороги, зелена=чорнило/хіл, жовтий маркер=підсвітка.
- Уся механіка в клітинках → геймплей однаковий на будь-якому екрані; малюнок масштабується окремо від хітбокса.
- **Перекрити прохід повністю не можна** — гра не дасть поставити останню перешкоду.
- **Чорнило:** стартовий запас + за вбитих ворогів.
- **Ластик повертає 50%** від ціни, і за стіни, і за башти.
- **Башта 2×2, стіна 1×1.**
- **Ввід:** тап ставить одну клітинку, протягування малює риску, поки вистачає чорнила.
- **Один вхід згори, база знизу.**
- **Малювати й стирати можна будь-коли**, навіть посеред хвилі.
- **Маршрут показуємо завжди** пунктиром, перемальовується при кожній зміні.
- Формат збірки — під вимоги Poki / CrazyGames.
- v1: 2 башти (магічна + гармата) + 1 ворог.

---

## 1. Ядро геймплею: ручка, ластик, чорнило

**Поле** — сітка клітинок ядра (15×26). Згори вхід, знизу база. Земля продовжується за ядро й заповнює екран; будувати можна і там.

**Ручка** тримає чорнило — це і валюта, і індикатор. Обраний інструмент малює:

| | розмір | ціна | що робить |
|---|---|---|---|
| Стіна | 1×1 | 5 | перекриває клітинку, подовжує маршрут |
| Магічна башта | 2×2 | 60 | б'є одиночну ціль, теж перекриває клітинки |
| Гармата | 2×2 | 100 | навісний постріл зі сплешем |

**Ластик** стирає будь-що намальоване і повертає **50%**. Стерти можна і посеред хвилі — маршрут одразу перерахується.

**Чорнило** приходить зі старту (120) і за кожного вбитого (вухань дає 8). Рівень чорнила видно **всередині ручки** в HUD: колба порожніє, коли малюєш, і доливається, коли когось убили.

**Маршрут.** Один BFS від бази по вільних клітинках дає поле відстаней; вороги просто йдуть униз по градієнту. Наслідки безкоштовні: перерахунок після кожної зміни — один прохід по 390 клітинках; вороги посеред шляху самі розвертаються; «чи можна тут будувати» — це те саме питання «чи лишився вхід досяжним» у тому самому полі.

**Заборона замикання.** Перед постановкою пробуємо клітинки в копії сітки й перераховуємо досяжність. Якщо вхід більше не веде до бази — місце підсвічується червоним і постановка не відбувається. Те саме, якщо в клітинці стоїть ворог.

Рух — по 4 напрямках (без діагоналей): так лабіринт читається на клітинці й виглядає накресленим по лініях зошита.

Хітбокс ворога 1.5 клітинки при русі по клітинковій сітці означає, що малюнок трохи перекриває стіни. У зошиті малюнок і так вилазить за клітинки — лишаємо як є.

---

## 1a. Башти: обводиш рукою, складаєш із деталей

### Петля

1. Перерва між хвилями, у пері є чорнило.
2. Ставиш **заготовку** башти або відкриваєш готову.
3. На полях аркуша — доступні деталі, як ескізи на берегах. Тягнеш деталь на місце кріплення.
4. Відкривається **велика рамка** з блідим олівцевим контуром. **Обводиш пальцем.**
5. Лінія переобводиться начисто, рамка стискається, деталь стає на башту й оживає.
6. Нотатка на полях: що виросло, як обвів, чи склалося комбо.

### Як читається обведення (`trace.js` ✔)

Штрихи гравця ресемпляться рівномірно, тому частота подій вказівника не впливає. Найближча точка контуру шукається перебором — контур × лінія це ~20 тис. операцій, поле відстаней було б передчасною оптимізацією.

- `accuracy` = `1 − середнє(clamp(d / tol))`, `tol` = 0.08 рамки;
- `coverage` = частка вузлів контуру, пройдених ближче `tol`;
- `speedBonus` — довжина контуру / час, відносно `refSpeed`;
- `extraLifts` — зайві відриви пальця понад кількість штрихів контуру.

```
cov'    = (coverage − minCoverage) / (1 − minCoverage)      // від порога, не від нуля
quality = clamp(0.55*accuracy + 0.35*cov' + 0.10*speedBonus − 0.05*extraLifts)
```

`coverage < 0.6` — не зараховано: чорнило не списується, пропонуємо переобвести. Це єдина відмова.

**Множник до статів деталі:** `0.7 + 0.55 * quality`. Заміряний розкид: ідеальне обведення ×1.23, абияк ×1.12, ледь зараховане ×0.74.

Оцінка не залежить від напрямку обведення й порядку штрихів.

**Магніт:** кожну точку тягнемо до контуру `p' = lerp(p, nearest, 0.7)`, далі переобведення повним пером. Не приймаємо руку як є і не підміняємо шаблоном — саме `magnet` вирішує баланс «моє / красиве». Це головне число для крутіння на живому пристрої, разом із `tol`.

### Рамка (`tracepad.js` ✔)

Великий квадрат `min(w,h) * 0.72` по центру. Навколо все гасне, всередину кладеться **чистий аркуш із власною клітинкою** — підкладка малюється окремим шаром зі звичайним блендом, бо шар чорнила множиться на папір, а множення вміє тільки темнити.

Кнопки «готово» немає: обвів усе (`coverage ≥ 0.97`) — зараховуємо одразу, обвів достатньо — після паузи в руці (700 мс). Тап повз рамку скасовує. Поки рамка відкрита, поле не приймає ввід, але час іде — обводити посеред хвилі можна й ризиковано.

З `?debug` результат тримається 8 секунд замість 0.8.

### Склад башти (`build.js` ✔)

Деталь має: контур, малюнок, **сокети**, ціну, стати, теги (`sharp`, `round`, `barrel`, `heavy`, `face`…). Заготовка дає перші сокети, кожна поставлена деталь приносить свої. Координати — клітинки, вісь y вниз, початок — низ заготовки.

```
cost   = Σ part.cost                                  // від якості НЕ залежить
damage = Σ (part.damage * q) * (1 + 0.15 * spikes)    // q = множник обведення
rate   = base.rate * (1 + 0.10 * round)
range  = base.range + 0.6*height + 0.4*topMass + Σ part.range*q
splash = Σ part.splash * q
crit   = 0.05 + 0.20 * symmetry
```

`height` — висота силуету, `topMass` — наскільки високо сидить маса над низом, `symmetry` — чи рівно заповнені боки. **Саме `height` і `topMass` роблять позицію деталі геймплеєм:** ствол угорі бʼє помітно далі за той самий ствол збоку.

**Комбо** — таблиця пар тегів: пружина + ствол = рикошет, морда + прапор = аура страху, колеса + дах = стріляє в два боки. Ніде не написані, знаходяться самі; у момент спрацювання на полях зʼявляється рукописна помітка.

`rigDef()` віддає дерево частин у форматі `rig.js`: зсув бере сокет **батька**, кожна деталь дістає свою фазу `sway`, деталі з тегом `barrel` — ще `aim` і `recoil`.

### Набір деталей v1 ✔

Одинадцять: **заготовки** — тумба (`stump`), бочка (`cask`); **зброя** — гармата, арбалет, посох; **верхівки** — дах, прапор; **оздоблення** — колеса, пружина, шипи, морда.

Заготовки дають сокети `top`, `left`, `right`, `face` (на корпус) і `bottom` (під низ, як у візка); дах дає ще один `top` під прапор. Решта — листя.

Заміряно на чотирьох складах із того самого набору: шкода 0…27.6, радіус 3.65…5.85, темп 1.0…1.4, комбо `fear` і `ricochet` спрацьовують. Склад без зброї дає нуль шкоди — це не баг, а башта підтримки.

Спершу малює `procart.js`, той самий генератор віддає й контур. Далі кожна деталь заміщується малюнком із зошита — і тут арт-конвеєр перестає бути допоміжним: **кожен новий намальований елемент — це нова деталь і новий білд**.

---

## 2. Візуал: як зробити, щоб це виглядало як реальний зошит

Шарами знизу вгору.

**Папір** (статичний, один раз): тепло-сірий `#f6f2e7`, м'які плями на multiply, тінь від згину зліва, загнутий правий нижній кут, зерно волокон на 12 fps.

**Клітинка**: колір `#a7bad6`, лінії майже рівні, але кожна має власний натиск, повільну хвилю і зрідка «недодрукований» шматок. Червона лінія полів тримається лівого краю аркуша.

**Чорнило** — усе ігрове:
- **multiply на шарі**, не на окремих Graphics: інакше сегменти множаться самі з собою і будь-яка ручка сідає в чорне (перевірено);
- частини — **маски**, колір дає `tint`, тому одна й та сама деталь годиться будь-якій ручці;
- лінія: плавний дрож (сума двох синусів), натиск уздовж штриха, ореол розтікання, перехльости на кутах;
- заливок немає, тільки штрихування; суцільне — лише жовтий маркер.

**Поля (HUD)**: рукописні підписи, життя рисочками, які закреслюються червоним. **Ручка з чорнилом і ластик** — намальовані об'єкти на полях, обраний інструмент трохи більший і підкреслений маркером.

**Boil**: `DisplacementFilter` низькочастотним шумом на шар чорнила (папір не чіпаємо), кадр міняється дискретно на 12 fps.

**Атмосфера:**
- **стіна не з'являється — вона домальовується** пером за ~0.2 с, башта за ~0.4 с;
- **ластик стирає з розмазуванням**: лишається бліда пляма, яка тане кілька секунд;
- **маршрут перекреслюється заново** щоразу, як ти щось поставив — це і підказка, і головний трюк стилю;
- смерть ворога — його **зачеркують** кількома зигзагами;
- цифри шкоди рукописні, червоні, спливають угору;
- влучання гармати — бризки чорнила.

---

## 3. Арт-пайплайн: фото → єдиний стиль → частини

```
art/raw/*.jpg  →[кроп+перспектива]→ art/crops → [restroke | AI] → art/clean → [маска] → art/parts
```

**Локальний restroke** (за замовчуванням, безкоштовно): контури через OpenCV, перемальовані нашим пером. Стиль збігається на 100%, бо малює наш рендер.

**AI-редизайн** (кнопка): `POST https://generativelanguage.googleapis.com/v1beta/interactions`, `gemini-3.1-flash-image` (~$0.067 за 1K) у роботі, `gemini-3-pro-image` для еталонів. Ключ у `tools/.env.local`, запити через локальний проксі — у браузер і в білд не потрапляє. Кеш за хешем, батч-режим для перегону всього.

**Три замки єдиного стилю:**
1. текстовий канон у `data/style.md` — незмінна шапка кожного запиту; головне в ній: **чистий білий фон, без паперу, без клітинки, без кольору** — це малює рушій;
2. 3 style-anchor зображення в кожному запиті, згенеровані з твого ж скетчу, тож анкор успадковує твій почерк;
3. детермінований нормалізатор `threshold → маска → tint`: дрейф кольору й тону фізично неможливий.

---

## 4. Редактор (`tools/editor/`)

Окрема сторінка, у білд не входить, пише файли через File System Access API. Готове: OpenCV.js, Pixi (прев'ю тим самим рантаймом), нативні слайдери.

- **Чорнило** — фото → перспектива → adaptiveThreshold → видалення клітинки → авторозріз на частини → імена; перемикач оригінал/restroke/AI і лічильник витрат.
- **Риг** — дерево частин, піво́ти мишкою, якорі (`muzzle`, `hit`, `ground`).
- **Анімація** — модифікатори (`bob`, `sway`, `swing`, `recoil`, `squash`, `wobble`, `spin`, `aim`, `blink`) зі слайдерами амплітуди/частоти/фази і тригерами; кнопки станів.
- **Палітра** — ручка на частину, глобальні слайдери вигляду → `look.json`.
- **Рівень** — вхід, база, заздалегідь намальовані стіни, декор, таблиця хвиль, ціни й баланс; кнопка «Грати» запускає справжню гру в панелі.

---

## 5. Файли

```
index.html            канвас + SDK-тег
lib/pixi.min.mjs      вендорений Pixi 8 (без CDN — вимога платформ)
src/main.js           boot, розкладка в клітинках, тікер, пауза          ✔
src/paper.js          папір, клітинка, поля, згин, зерно, віньєтка       ✔
src/ink.js            примітиви пера, boil, ізольований шар чорнила      ✔
src/rig.js            cut-out риг + процедурні модифікатори              ✔
src/procart.js        12 дудл-плейсхолдерів                              ✔
src/pathmath.js       полілінія: точка на відстані, дистанція до шляху   ✔
src/grid.js           зайнятість клітинок: стіни, башти, межі             ✔
src/flow.js           BFS від бази, поле напрямків, перевірка замикання   ✔
src/tools.js          ручка й ластик: тап, протягування, привид, ціни     ✔
src/economy.js        чорнило: ціни, списання, 50% повернення             ✔
src/combat.js         таргетинг, снаряди, сплеш, закреслення, цифри       ✔
src/game.js           вороги, хвилі, бій, чорнило, життя, win/lose       ✔
src/hud.js            ручка з рівнем, лічильники, фінальна записка        ✔
src/sdk.js            шим Poki / CrazyGames                              ✔
src/trace.js          оцінка обведення, магніт (без Pixi)                ✔
src/tracepad.js       рамка: контур, палець, переобведення, оцінка       ✔
src/build.js          склад башти: сокети, виміри, стати, комбо, гармата ✔
src/workshop.js       деталі на полях, перетягування, стиснення          ✔
src/selfcheck.js      node-asserts: pathmath ✔ flow ✔ ціни ✔ обведення ✔ склад ✔ заготовки ✔
data/*.json           look ✔ parts ✔ rigs ✔ level ✔ balance ✔
data/towerparts.json  деталі: контури, сокети, ціни, стати, теги, комбо     ✔
tools/serve.js        статичний сервер (+ проксі до Gemini)              ✔
tools/editor/         редактор
```

`level.json` більше не містить траси: тільки вхід, базу, стартові стіни, декор і хвилі.

### Баланс v1
Старт 120 чорнила, 10 життів, 8 хвиль (5→20 ворогів, живучість ×1 → ×3.2, перерва 6 с). Стіна 5, магічна башта 60 (12 шкоди, 1.1/с, радіус 4.2), гармата 100 (30 шкоди, 0.55/с, радіус 3.6, сплеш 1.5). Вухань: 40 HP, 1.5 клітинки/с, дає 8 чорнила. Ластик повертає 50%.

**Заміряно:** «гравець», що купує магічну башту щоразу, як назбирав 60, проходить усі 8 хвиль за 3.5 хв, не втративши жодного життя (93 вбитих, 14 башт). Без жодної башти поразка настає на 62-й секунді. Тобто верхня межа складності зараз надто низька — цифри хвиль треба піднімати, коли зʼявиться другий тип ворога.

---

## 6. Кроки

Зроблено: **1** аркуш ✔, **2** перо й boil ✔, **3** риг і дудли ✔, **4** сітка й маршрут ✔,
**5** ручка й ластик ✔, **6** чорнило й HUD ✔, **7** бій ✔, **8** цикл ✔.

4. **Сітка й маршрут.** `grid.js`, `flow.js`, BFS-поле, перевірка замикання, пунктирний маршрут, що перемальовується; ворог іде по градієнту й б'є базу. `selfcheck.js` на flow. *Перевірка:* ставлю стіну з консолі — маршрут одразу інший.
5. **Ручка й ластик.** `tools.js`: вибір інструмента, тап і протягування, привид під пальцем, жовта підсвітка валідних клітинок, червона — там, де замкнуло; домальовування і стирання з плямою. *Перевірка:* лабіринт будується пальцем, замкнути неможливо.
6. **Чорнило й HUD.** ✔ `economy.js` + `hud.js`: ручка з рівнем, ластик, життя рисочками; ціни, повернення 50%, чорнило за вбитих. HUD сідає в ту вільну смугу, що товща: у портреті знизу, у ландскейпі зліва. Цифри пишуться нашим пером (гліфи в `ink.js`), шрифт не вендоримо.
7. **Бій.** ✔ `combat.js`: ціль обирається за полем відстаней — б'ємо того, хто ближчий до бази, а не до башти. Магія веде ціль, ядро летить навісом у точку й дає сплеш. Смерть — закреслення, шкода — рукописні цифри.
8. **Цикл.** ✔ 8 хвиль, наступна — коли поле чисте (перерва = час домалювати лабіринт). Фінальна записка поверх аркуша: галочка або закреслено, скільки хвиль, стрілка «ще раз»; тап будь-де = заново (`location.reload`). `sdk.js` мовчить, поки платформного тега немає.

### Друга частина: башти, які обводить гравець

Нумерація своя, бо це окрема фіча поверх готової гри.

1. **`trace.js` + тести.** ✔ Оцінка обведення й магніт на синтетичних лініях, без UI.
2. **Рамка обведення.** ✔ `tracepad.js`: чистий аркуш у рамці, олівцевий контур, живе прев'ю посегментно (перемальовувати не можна — перо щоразу кидає новий дрож і лінія кипить), переобведення начисто, оцінка у відсотках. *Перевірено:* синтетична рука дає 0.96 / 0.76 / 0.08 на точному, недбалому й ледь зарахованому обведенні; поле під рамкою не приймає ввід.
3. **`build.js` + тести.** ✔ Склад → виміри → стати → комбо → `rigDef`. *Перевірено:* та сама деталь у верхньому сокеті дає більший радіус, ніж у бічному; зняття забирає все, що трималось зверху; комбо живе лише поки обидві деталі на місці.
4. **Одинадцять деталей.** ✔ `towerparts.json` + `bakeCatalogue` у `procart.js`. **Контур — єдине джерело:** з того самого набору ліній печеться і те, що гравець обводить, і те, що стає на поле, тому вони не можуть розійтись. Заготовки мають пʼять сокетів (`top`, `left`, `right`, `face`, `bottom`), бо перші три давали купу мотлоху одна на одній. Коробка деталі рахується від її піво́та — інакше горизонтальний ствол «додавав би висоти» й дурив формулу радіуса.
5. **Майстерня.** ✔ `workshop.js`: тап по башті → ескізи деталей на полях → перетягування → підсвітка сокетів жовтим → рамка обведення → обведене летить на башту, стискаючись → списання чорнила. `tools.js` віддає тап по башті, `game.js` тримає склад кожної башти й перезбирає риг після кожної деталі.

   **Панель інструментів більше не ставить готові башти, а лише заготовки:** рецепт у `towerparts.templates` (магічна = тумба + посох, гармата = бочка + ствол), далі все дороблює гравець. Через це ригі `magic_tower` і `cannon` з `rigs.json` більше не використовуються (самі описи поки лишились) — башта на полі тепер завжди зібрана з тих самих деталей, які гравець обводить.

   **Тип снаряда більше не записаний у таблиці, а рахується зі складу** (`gunOf` у `build.js`): є сплеш — летить ядром, немає — згустком. Тому башта міняє характер, коли з неї знімають зброю, а не коли їй міняють назву.

   *Перевірено синтетичним пальцем:* тап по башті відкриває майстерню з 9 ескізами і 4 вільними сокетами; перетягування шипів на лівий сокет відкриває рамку; точне обведення дає ×1.25 до статів; після нього склад `[тумба, посох, шипи]`, шкода 11.25 → 17.25, чорнило 660 → 650, стати гармати в `combat` оновлені. Тап, яким майстерню закривають, **не** малює стіну під панеллю — дотик дістається майстерні цілком (обробник поля стоїть у черзі за нашим).
   
   Заодно: у наборі гліфів не було скісної риски, тож лічильник хвиль «1/8» малювався як «1 8»; і сам лічильник переїхав із центру вільної смуги в куток, бо центр тепер займає панель деталей.
6. **Комбо й нотатка на полях:** стрілки з іконкою й цифрою (око → радіус, шпичак → шкода, спіраль → темп, коло → сплеш) на 4 секунди, далі за тапом.
7. **Баланс** на повному прогоні 8 хвиль; крутимо `magnet` і `tol` на живому телефоні.

### Далі

8. **Арт-конвеєр** + перший anchor-набір із фото зошита. Тепер він критичний: кожен намальований елемент — нова деталь.
9. **Редактор**: Чорнило → Риг → Анімація → Палітра → Рівень → **Деталі** (контури й сокети).

---

## 7. Verification

- `node tools/serve.js 5183` (запускається і через `.claude/launch.json`), грати на 375×812 і 812×375 — обидві орієнтації грабельні.
- `node src/selfcheck.js` — pathmath, BFS-поле, детект замикання, Брезенхем, арифметика цін, оцінка обведення й магніт, склад башти, тип снаряда зі складу, збірка всіх заготовок із каталогу. Мовчазний вихід 0 = все гаразд.
- DevTools Network: нуль зовнішніх запитів, крім тега SDK.
- Два повні прогони: перемога і поразка, обидва з рестартом.
- Візуальний A/B зі скетчем у зошиті.

**Як перевіряти в браузері.** Скріншоти прев'ю віддають кадр, що не збігається з вікном, тому геймплей перевіряється числами: `window.__td` віддає `app, layout, game, tools, hudUi, tracePad, sdk, DEMO`, а ввід емулюється синтетичними `PointerEvent` у координатах, порахованих із `layout`. Приклади — в історії кроків 5–8 і в другій частині.

## 8. Відкриті питання

1. **Чи блокують башти рух?** Поки закладаю «так» — башта 2×2 працює і як стіна, тому лабіринт можна вести самими баштами. Скажи, якщо ні.
2. Межа автомасштабу спрайтів (зараз 1.8×) — покрутимо на телефоні.
3. Звук: синтезувати WebAudio чи чекати записів. Поки без звуку.
4. Рукописний шрифт вендорити локально чи малювати цифри дудлом. Поки дудл.
5. Другий тип ворога і бос — у v2 чи одразу.
6. **`tol` і `magnet`** — розкид між точним і недбалим обведенням зараз лише 9%. Синусоїда замість руки цього не покаже; крутити на живому пальці.
7. **Скільки деталей на башту** до того, як вона займе пів екрана. Розмір спрайта відвʼязаний від хітбокса, тому вирішується множником, але межу треба намацати.
8. **Чи лишаються стіни взагалі.** Вирішено: лишаються, дешеві й другорядні. Якщо після майстерні виявиться, що їх ніхто не малює — прибрати.
